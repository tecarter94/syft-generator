apiVersion: v1
kind: Service
metadata:
  name: {{ include "syft-generator.fullname" . }}
  labels:
    {{- include "syft-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syft-generator.selectorLabels" . | nindent 4 }}
